### React Context Schemas Directory

The `schemas` directory contains the data contracts used by the React context.
Its only responsibility is to define the expected shape of input, output, form,
and view data.
