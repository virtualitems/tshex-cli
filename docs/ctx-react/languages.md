### React Context Languages Directory

The `languages` directory contains the language resources for the React
context. Its only responsibility is to define user-facing text for each locale.
Each resource uses the same message keys as the other locale resources.
