### React Context Hooks Directory

The `hooks` directory contains React hook modules that encapsulate reusable
interface behavior. Its only responsibility is to expose that behavior to
components without rendering interface elements.
